import 'package:flutter/material.dart';

void main() => runApp(const MaterialApp(
  home: Scaffold(
    backgroundColor: Colors.black,
    body: Center(
      child: Text("ZENITH OS ONLINE", 
      style: TextStyle(color: Colors.red, fontSize: 24, fontWeight: FontWeight.bold)),
    ),
  ),
));
