name: zenith_os
description: "Advanced AI Super-App with Device Control."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  google_generative_ai: ^0.4.0
  # Shizuku fix: Direct link from GitHub
  shizuku_channel:
    git:
      url: https://github.com/RikkaApps/shizuku-api.git
      path: flutter/shizuku_channel
  pdf: ^3.10.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
