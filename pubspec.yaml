name: zenith_os
description: "Advanced AI Super-App with Device Control."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  google_generative_ai: ^0.4.0 # Gemini API integration
  shizuku_channel: ^1.1.0      # Device Control integration
  pdf: ^3.10.8                 # CV/Resume PDF Export
